# GitHub Secrets Template
# Format: KEY=value
# Each secret must have a level marker: @REPO or @ENV
# @REPO = Repository secret (shared across all workflows)
# @ENV = Environment secret (production-critical, requires approval)

# ============================================================================
# ENVIRONMENT-LEVEL SECRETS (Production-critical, requires approval)
# Upload with: .\upload-env-secrets.ps1
# ============================================================================

# Cloudflare (deployment)
CLOUDFLARE_ACCOUNT_ID=@ENV
CLOUDFLARE_API_TOKEN=@ENV

# Database (production connection)
DATABASE_URL=@ENV

# Authentication (session encryption)
NEXTAUTH_SECRET=9E7k2L5qP8vR3tN6xB1mC4wJ9fD7sA2g+H8jK6lM3n@ENV

# ============================================================================
# REPOSITORY-LEVEL SECRETS (Shared, non-sensitive)
# Upload with: .\upload-repo-secrets.ps1
# ============================================================================

# NextAuth (app URL)
NEXTAUTH_URL=@REPO

# Email (Brevo)
BREVO_API_KEY=@REPO
RESEND_FROM_EMAIL=noreply@pratibhaparishad.in@REPO

# Telegram (optional)
TELEGRAM_BOT_TOKEN=@REPO

# Payments (optional)
RAZORPAY_KEY_ID=@REPO
RAZORPAY_KEY_SECRET=@REPO

# ============================================================================
# LOCAL DEVELOPMENT ONLY (Do NOT upload to GitHub)
# ============================================================================

# SMTP (Brevo) - for local development only
SMTP_HOST=smtp-relay.brevo.com@LOCAL
SMTP_PORT=587@LOCAL
SMTP_LOGIN=ad1011001@smtp-brevo.com@LOCAL
SMTP_PASSWORD=L8FIXpJNZ695SV0j@LOCAL
